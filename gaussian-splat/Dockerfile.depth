FROM pytorch/pytorch:2.1.0-cuda11.8-cudnn8-runtime

RUN pip install --no-cache-dir \
    runpod>=1.6 \
    transformers>=4.40 \
    Pillow \
    numpy \
    accelerate

WORKDIR /app
COPY handler_depth.py .

CMD ["python", "-u", "handler_depth.py"]
